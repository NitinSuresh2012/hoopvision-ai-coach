# Runtime
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
numpy==1.26.4
opencv-contrib-python==4.11.0.86
# 0.10.21 retains the MediaPipe Pose Solutions graph used by pose_engine.py.
# Newer 0.10 releases removed `mp.solutions` from the Python wheel.
mediapipe==0.10.21
# Pinning avoids pip backtracking across newer JAX releases that require
# NumPy 2; MediaPipe declares JAX even though this Pose graph does not call it.
jax==0.4.38
jaxlib==0.4.38

# Tests
pytest>=8,<9
httpx>=0.27,<1
